create table if not exists season ( nKey           integer primary key,
                                    nAnimeLink     integer,
                                    nSeasonNumber  integer,
                                    szURL          varchar(255)
);